-- Deployed follow-up migration that resolved a PL/pgSQL variable-name collision.
-- The corrected function definitions are consolidated in the preceding repository migration
-- so fresh environments receive the final contract in one pass.

select 1;
